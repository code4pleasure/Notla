export default class Helper {
    static setTabBarName(routeName) {
        if(routeName === '') {
            
        }
    }

    static setTabBarIcon(routeName) {
        if(routeName === '') {

        }

    }

    static setSelectedTabBarIcon(routeName) {
        if(routeName === '') {

        }
    }
}